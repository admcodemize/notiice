import { IDropdownProps } from "../../../assets/types/components/dropdown/Global";

export const Inbox = ({ callback }: IDropdownProps): JSX.Element => {
    return (
        <div><span>Inbox</span></div>
    )
}