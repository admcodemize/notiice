import { StyledWorkflow } from "../../assets/styles/components/sliderLeft/Workflow.styles";

export const Workflow = (): JSX.Element => {
    return (
        <StyledWorkflow>
            <span>Workflow</span>
        </StyledWorkflow>
    )
}