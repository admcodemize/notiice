import { StyledEventTypes } from "../../../assets/styles/components/sliderLeft/schedulePages/EventTypes.styles";

export const EventTypes = (): JSX.Element => {
    return (
        <StyledEventTypes>
            <span>event types</span>
        </StyledEventTypes>
    )
}