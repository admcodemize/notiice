import { StyledDashboard } from "../../assets/styles/components/content/Dashboard.styles";

export const Dashboard = (): JSX.Element => {
    return (
        <StyledDashboard>
            <span>Dashboard</span>
        </StyledDashboard>
    )
}