export type TConstRoleProps = {
    FREE: number[]
}

export type TConstRouteProps = {
    DASHBOARD: string,
    WORKFLOW: string,
    WORKSCHEDULE: string,
    CALENDAR: string,
    INTEGRATIONS: string
}